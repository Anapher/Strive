import React from 'react';
import { UseFormMethods } from 'react-hook-form';
import { ConferenceDataForm } from '../form';

type Props = {
   form: UseFormMethods<ConferenceDataForm>;
};

export default function TabPermissions({ form }: Props) {
   return <div></div>;
}
