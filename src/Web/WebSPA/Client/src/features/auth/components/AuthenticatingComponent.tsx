import React from 'react';
import BaseAuthComponent from './BaseAuthComponent';

export default function AuthenticatingComponent() {
   return <BaseAuthComponent title="Authenticating" text="You will be redirected to the login page" />;
}
